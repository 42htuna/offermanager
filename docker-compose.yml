services:
  teklif-service:
    image: teklif:latest
    container_name: teklif
    build: .
    restart: unless-stopped
    ports:
      - "8081:8000"
    volumes:
      - teklif:/opt/offermanager/data

volumes:
  teklif:
    name: teklif
